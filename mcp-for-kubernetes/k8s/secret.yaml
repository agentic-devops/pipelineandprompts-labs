# DO NOT commit real values. Use this as a template.
# Populate via: kubectl create secret generic platform-mcp-secrets \
#   --from-literal=api-key=<YOUR_KEY> -n platform-tools
apiVersion: v1
kind: Secret
metadata:
  name: platform-mcp-secrets
  namespace: platform-tools
type: Opaque
stringData:
  api-key: "REPLACE_ME"  # Replace before applying — never commit real keys
