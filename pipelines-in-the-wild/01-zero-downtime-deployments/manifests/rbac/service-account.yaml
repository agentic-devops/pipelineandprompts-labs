apiVersion: v1
kind: ServiceAccount
metadata:
  name: pipeline-deployer
  namespace: zero-downtime-demo
  labels:
    app.kubernetes.io/managed-by: pipeline
    project: zero-downtime-deployments
  annotations:
    # Document the purpose of this service account
    description: "Minimum-privilege service account for GitHub Actions blue/green pipeline"
